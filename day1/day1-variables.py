name = "Jack"
print(name)

name = input("What is your name?")
length = len(name)
print(length)

#variables names:
#never use spaces # give meaninful names # use undescore
#for double names as user_name # you can use number but by the end of the variable
